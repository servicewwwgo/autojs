# AutoJS 项目文档

## 目录
1. [项目概述](#项目概述)
2. [源代码文件详细说明](#源代码文件详细说明)
3. [项目功能特性](#项目功能特性)
4. [项目结构](#项目结构)
5. [安装和运行](#安装和运行)
6. [注意事项](#注意事项)

---

## 项目概述

AutoJS 是一个基于 WXT + Vue 3 + TypeScript 的 Chrome 浏览器扩展，用于实现 Web 自动化爬虫功能。项目通过 Chrome DevTools Protocol (CDP) 协议控制浏览器，支持元素查找、鼠标键盘操作、页面导航、截图等功能，并通过 WebSocket 与服务器进行通信。

---

## 源代码文件详细说明

### 1. 入口文件 (Entrypoints)

#### 1.1 `src/entrypoints/background.ts`
**功能**: Background Script 入口文件，扩展的后台服务核心

**作用**:
- 初始化指令执行器和 CDP 执行器
- 处理来自 popup 和 content script 的消息
- 管理标签页生命周期（激活、更新、关闭）
- 监听 CDP 事件（控制台日志、网络日志）
- 管理 WebSocket 连接和消息处理
- 处理指令的添加和执行

**关键数据结构**:
- `mapTypeToFunction`: 消息类型到处理函数的映射
- `instructionExecutor`: 指令执行器实例
- `cdpExecutor`: CDP 执行器实例
- `wsConnector`: WebSocket 连接器实例

**主要功能函数**:
- `add_example_instructions()`: 添加示例指令
- `get_tabs()`: 获取所有标签页
- `add_instructions()`: 添加指令集
- `execute_instructions()`: 执行指令集
- `pause_execution()`: 暂停执行
- `stop_execution()`: 停止执行
- `connect_websocket()`: 连接 WebSocket
- `send_results_to_server()`: 发送结果到服务器

#### 1.2 `src/entrypoints/content.ts`
**功能**: Content Script 入口文件，在页面上下文中运行

**作用**:
- 隐藏 `navigator.webdriver` 属性，防止网站检测自动化工具
- 提供 DOM 元素查找功能（CSS、XPath、ID 选择器）
- 提供元素属性获取功能（支持标准属性、计算样式、图片等）
- 提供元素可见性检查功能
- 提供元素滚动到视图功能
- 与 background script 通信，通知内容脚本已加载

**关键数据结构**:
- `mapTypeToFunction`: 消息类型到处理函数的映射
- `ElementTag`: 元素标记常量 `'cdp-locate-id'`

**主要功能函数**:
- `hideWebdriver()`: 隐藏 webdriver 属性
- `FindElement()`: 查找元素（支持 CSS、XPath、ID）
- `FindElementByTag()`: 通过标记查找元素
- `GetAttribute()`: 获取元素属性
- `GetText()`: 获取元素文本
- `IsVisible()`: 检查元素是否可见（全面的可见性检查）
- `CheckElementVisible()`: 检查元素可见性的消息处理函数

#### 1.3 `src/entrypoints/popup/main.ts`
**功能**: Popup 窗口入口文件

**作用**:
- 初始化 Vue 3 应用
- 挂载 App.vue 组件到页面

**关键数据结构**: 无

#### 1.4 `src/entrypoints/popup/App.vue`
**功能**: Popup 窗口主组件

**作用**:
- 提供标签页导航界面
- 集成四个主要功能模块：节点配置、指令配置、执行控制、执行日志

**关键数据结构**:
- `activeTab`: 当前激活的标签页 ID
- `tabs`: 标签页配置数组

**组件**:
- `NodeConfig`: 节点配置组件
- `InstructionConfig`: 指令配置组件
- `ExecutionControl`: 执行控制组件
- `ExecutionLog`: 执行日志组件

---

### 2. Popup 组件 (Popup Components)

#### 2.1 `src/entrypoints/popup/components/NodeConfig.vue`
**功能**: 节点配置管理组件

**作用**:
- 显示和编辑节点配置信息（节点类型、ID、名称、令牌）
- 从 background script 加载配置
- 保存配置到 background script

**关键数据结构**:
- `profile`: 节点配置对象（NodeProfile 类型）
  - `node_type`: 节点类型（固定为 'crawler'）
  - `node_id`: 节点ID（自动生成，不可修改）
  - `node_name`: 节点名称（可编辑）
  - `node_token`: 节点令牌（可编辑）

#### 2.2 `src/entrypoints/popup/components/InstructionConfig.vue`
**功能**: 指令配置管理组件

**作用**:
- 选择目标标签页
- 输入和验证指令集 JSON
- 发送指令到 background script
- 加载示例指令

**关键数据结构**:
- `selectedTabId`: 选中的标签页ID
- `instructionsJson`: 指令集 JSON 字符串
- `tabs`: 标签页列表

#### 2.3 `src/entrypoints/popup/components/ExecutionControl.vue`
**功能**: 执行控制组件

**作用**:
- 管理 WebSocket 连接（连接、断开、测试）
- 控制指令执行（开始、暂停、停止）
- 显示执行状态（运行状态、暂停状态、执行统计）

**关键数据结构**:
- `wsUrl`: WebSocket URL
- `isConnected`: WebSocket 连接状态
- `selectedTabId`: 选中的标签页ID
- `executorStatus`: 执行器状态对象
  - `isRunning`: 是否正在运行
  - `isPaused`: 是否暂停
  - `executedCount`: 已执行数量
  - `successCount`: 成功数量
  - `errorCount`: 失败数量
  - `startTime`: 开始时间

#### 2.4 `src/entrypoints/popup/components/ExecutionLog.vue`
**功能**: 执行日志查看组件

**作用**:
- 显示指令执行结果日志
- 支持过滤（仅显示成功/失败）
- 清空日志
- 发送日志到服务器

**关键数据结构**:
- `logs`: 指令结果数组（InstructionResult[]）
- `filterSuccess`: 是否仅显示成功
- `filterError`: 是否仅显示失败

---

### 3. 执行器模块 (Executor)

#### 3.1 `src/executor/InstructionExecutor.ts`
**功能**: 指令执行器核心类

**作用**:
- 管理指令执行流程（FIFO 队列）
- 控制执行状态（运行、暂停、停止）
- 统计执行结果（成功数、失败数）
- 处理 WebSocket 消息中的指令

**关键数据结构**:
- `instructionManager`: 指令管理器实例
- `resultManager`: 结果管理器实例
- `isRunning`: 是否正在运行
- `isPaused`: 是否暂停
- `stopRequested`: 是否请求停止
- `executedCount`: 已执行数量
- `successCount`: 成功数量
- `errorCount`: 失败数量
- `startTime`: 开始时间

**主要方法**:
- `ExecuteAll()`: 执行所有指令（循环执行，FIFO 队列）
- `Pause()`: 暂停执行
- `Resume()`: 恢复执行
- `Stop()`: 停止执行
- `GetStatus()`: 获取执行状态
- `handleMessage()`: 处理 WebSocket 消息中的指令

#### 3.2 `src/executor/CdpExecutor.ts`
**功能**: CDP (Chrome DevTools Protocol) 执行器

**作用**:
- 执行 CDP 命令（列出标签页、元素截图、执行 JavaScript、源码搜索等）
- 收集和管理控制台日志
- 收集和管理网络日志
- 处理 WebSocket 消息中的 CDP 请求

**关键数据结构**:
- `consoleLogs`: 控制台日志映射（Map<tabId, log[]>）
- `networkLogs`: 网络日志映射（Map<tabId, log[]>）
- `MAX_LOG_ENTRIES`: 每个标签页最多保存的日志条目数（10000）
- `mapTypeToFunction`: CDP 消息类型到处理函数的映射

**主要方法**:
- `handleListTargets()`: 列出所有标签页
- `handleTakeElementScreenshot()`: 截取元素截图
- `handleExecuteJavaScript()`: 执行 JavaScript 代码
- `handleSendCommand()`: 执行通用 CDP 命令
- `handleGrepSource()`: 源码搜索
- `handleGetNetworkLogs()`: 获取网络日志
- `handleGetConsoleLogs()`: 获取控制台日志
- `addConsoleLog()`: 添加控制台日志
- `addNetworkLog()`: 添加网络日志

#### 3.3 `src/executor/WebSocketConnector.ts`
**功能**: WebSocket 连接器

**作用**:
- 管理与服务器的 WebSocket 连接
- 处理连接、重连、心跳
- 发送和接收消息
- 处理登录认证

**关键数据结构**:
- `ws`: WebSocket 实例
- `url`: WebSocket URL
- `reconnectInterval`: 重连间隔（5秒）
- `heartbeatInterval`: 心跳间隔（30秒）
- `connected`: 连接状态
- `isLoggedIn`: 登录状态
- `isDisconnecting`: 是否正在断开连接
- `mapMessageTypeToFunction`: 消息类型到处理函数的映射

**主要方法**:
- `connect()`: 连接 WebSocket
- `disconnect()`: 断开连接
- `sendMessage()`: 发送消息
- `testConnection()`: 测试连接
- `registerMessageTypeHandler()`: 注册消息类型处理器
- `sendLoginMessage()`: 发送登录消息

---

### 4. 指令模块 (Instructions)

#### 4.1 `src/instructions/BaseInstruction.ts`
**功能**: 基础指令抽象类

**作用**:
- 定义所有指令的通用属性和方法
- 提供延迟、重试、CDP 命令执行等通用功能
- 作为所有具体指令类的基类

**关键数据结构**:
- `tabId`: 标签页ID
- `type`: 指令类型
- `instructionID`: 指令ID
- `delay`: 延迟时间（秒）
- `retry`: 重试次数
- `timeout`: 超时时间
- `ignoreError`: 是否忽略错误
- `created_at`: 创建时间

**主要方法**:
- `Execute()`: 执行指令（抽象方法，由子类实现）
- `Delay()`: 等待指定时间
- `Retry()`: 重试执行指令
- `ExecuteCDPCommand()`: 执行 CDP 命令
- `SendMessageToContentScript()`: 发送消息到内容脚本

#### 4.2 `src/instructions/FindElementInstruction.ts`
**功能**: 元素查找指令

**作用**:
- 根据选择器查找页面元素
- 支持 CSS、XPath、ID 三种选择器类型
- 支持相对关系筛选（父元素、子元素、兄弟元素）
- 检查元素可见性
- 为找到的元素设置标记（tag）

**关键数据结构**:
- `elementData`: 元素数据对象（ElementData 类型）
  - `name`: 元素名称
  - `selector`: 选择器
  - `selectorType`: 选择器类型（'css' | 'xpath' | 'id'）
  - `parentName`: 父元素名称（可选）
  - `childrenName`: 子元素名称（可选）
  - `siblingName`: 兄弟元素名称（可选）
  - `siblingOffset`: 兄弟元素偏移量（可选）

**主要方法**:
- `FindAllMatchingElementNodeIds()`: 查找所有匹配的元素节点ID
- `checkNodeRelations()`: 检查节点相对关系
- `checkNodeIsVisible()`: 检查节点是否可见
- `selectElementByParent()`: 根据父元素筛选
- `selectElementByChildren()`: 根据子元素筛选
- `selectElementBySibling()`: 根据兄弟元素筛选

#### 4.3 `src/instructions/InputInstruction.ts`
**功能**: 文本输入指令

**作用**:
- 向输入框输入文本
- 支持清空输入框
- 支持逐字符输入（可设置延迟）
- 支持中文字符和 Unicode 字符

**关键数据结构**:
- `elementName`: 元素名称
- `text`: 要输入的文本
- `clear`: 是否清空输入框

#### 4.4 `src/instructions/KeyboardInstruction.ts`
**功能**: 键盘操作指令

**作用**:
- 模拟键盘按键操作
- 支持 press、type、keydown、keyup 四种操作
- 支持特殊按键（Enter、Escape、方向键等）
- 支持聚焦元素

**关键数据结构**:
- `elementName`: 元素名称（可选）
- `action`: 操作类型（'press' | 'type' | 'keydown' | 'keyup'）
- `key`: 按键名称或字符

**主要方法**:
- `executePress()`: 执行按键按下操作
- `executeType()`: 执行输入操作
- `executeKeyDown()`: 执行按键按下
- `executeKeyUp()`: 执行按键释放
- `isSpecialKey()`: 判断是否为特殊按键
- `getVirtualKeyCode()`: 获取虚拟键码

#### 4.5 `src/instructions/MouseInstruction.ts`
**功能**: 鼠标操作指令

**作用**:
- 模拟鼠标操作（点击、双击、右键点击、悬停、移动等）
- 支持鼠标轨迹模拟（calculated、simulated、none）
- 支持基于元素或坐标的鼠标操作

**关键数据结构**:
- `action`: 操作类型（'click' | 'dblclick' | 'rightclick' | 'hover' | 'left_mousedown' | 'left_mouseup' | 'right_mousedown' | 'right_mouseup' | 'move_to'）
- `elementName`: 元素名称（可选）
- `x`: X 坐标（可选）
- `y`: Y 坐标（可选）
- `simulate`: 轨迹模拟类型（'calculated' | 'simulated' | 'none'）

**主要方法**:
- `MoveMouseTo()`: 移动鼠标到目标位置
- `CalculatedMouseTrajectory()`: 计算鼠标轨迹（贝塞尔曲线）
- `SimulatedMouseTrajectory()`: 模拟鼠标轨迹（更真实的算法）
- `NoneMouseTrajectory()`: 不模拟轨迹，直接移动

#### 4.6 `src/instructions/GetAttributeInstruction.ts`
**功能**: 获取元素属性指令

**作用**:
- 获取元素的属性值
- 支持标准 HTML 属性、计算样式、图片等

**关键数据结构**:
- `elementName`: 元素名称
- `attribute`: 属性名称（可选，默认为 'text'）

#### 4.7 `src/instructions/NavigateInstruction.ts`
**功能**: 页面导航指令

**作用**:
- 导航到指定 URL
- 等待页面加载完成

**关键数据结构**:
- `url`: 目标 URL

**主要方法**:
- `WaitForPageLoad()`: 等待页面加载完成

#### 4.8 `src/instructions/ScreenshotInstruction.ts`
**功能**: 页面截图指令

**作用**:
- 截取页面截图
- 支持 PNG 和 JPEG 格式
- 支持设置图片质量

**关键数据结构**:
- `format`: 图片格式（'png' | 'jpeg'）
- `quality`: 图片质量（1-100，仅 JPEG）
- `fullPage`: 是否全页截图（暂未实现）

#### 4.9 `src/instructions/ExecuteScriptInstruction.ts`
**功能**: JavaScript 执行指令

**作用**:
- 在页面上下文中执行 JavaScript 代码
- 支持完整的 CDP Runtime.evaluate 参数

**关键数据结构**:
- `expression`: JavaScript 表达式
- `returnByValue`: 是否按值返回
- `awaitPromise`: 是否等待 Promise
- `userGesture`: 是否模拟用户手势
- 其他 CDP Runtime.evaluate 参数

#### 4.10 `src/instructions/index.ts`
**功能**: 指令模块统一导出和工厂类

**作用**:
- 导出所有指令类
- 提供指令工厂方法，根据指令类型创建对应的指令实例

**关键数据结构**:
- `InstructionFactory`: 指令工厂类
  - `create()`: 根据指令对象创建指令实例
  - `toObject()`: 将指令对象转换为普通对象

---

### 5. 管理器模块 (Managers)

#### 5.1 `src/managers/ElementManager.ts`
**功能**: 元素管理器

**作用**:
- 管理所有已找到的元素
- 按标签页ID和元素名称分组存储
- 提供元素的增删改查功能

**关键数据结构**:
- `elements`: 元素映射（Map<tabId, Map<name, IElement>>）
- `ElementClass`: 元素类实现
  - `elementData`: 元素数据对象
  - `Validate()`: 验证元素数据
  - `GetNodeId()`: 获取节点ID
  - `GetTag()`: 获取元素标记
  - `GetName()`: 获取元素名称

**主要方法**:
- `GetElementByName()`: 根据名称获取元素
- `SetElementByName()`: 保存元素
- `RemoveElementByName()`: 删除元素
- `ClearTabElements()`: 清空指定标签页的所有元素

#### 5.2 `src/managers/InstructionManager.ts`
**功能**: 指令管理器

**作用**:
- 管理所有待执行的指令
- 按标签页ID分组存储
- 确保指令按创建时间排序（FIFO 队列）

**关键数据结构**:
- `instructionsMap`: 指令映射（Map<tabId, BaseInstructionClass[]>）

**主要方法**:
- `AddUnfilteredInstructions()`: 添加未过滤的指令列表（自动分组）
- `AddInstructions()`: 添加指令列表到指定标签页
- `GetFirstInstructionByTabId()`: 获取第一个指令（FIFO）
- `GetInstructionsByTabId()`: 获取指定标签页的所有指令
- `DeleteInstructionsByTabId()`: 删除指定标签页的所有指令

#### 5.3 `src/managers/InstructionResultManager.ts`
**功能**: 指令结果管理器

**作用**:
- 保存所有指令的执行结果
- 按指令ID索引存储结果

**关键数据结构**:
- `results`: 结果映射（Map<instructionID, InstructionResult>）

**主要方法**:
- `SaveResult()`: 保存结果
- `GetResult()`: 获取结果
- `GetAllResults()`: 获取所有结果
- `ClearAll()`: 清空所有结果

#### 5.4 `src/managers/NodeConfig.ts`
**功能**: 节点配置管理器

**作用**:
- 管理节点配置信息
- 从浏览器本地存储读取和保存配置
- 自动生成节点ID（UUID）

**关键数据结构**:
- `nodeProfile`: 节点配置对象（NodeProfile 类型）
  - `node_type`: 节点类型
  - `node_id`: 节点ID（UUID）
  - `node_name`: 节点名称
  - `node_token`: 节点令牌

**主要方法**:
- `GetNodeProfile()`: 获取节点配置（从本地存储或生成）
- `UpdateNodeProfile()`: 更新节点配置

#### 5.5 `src/managers/TabManager.ts`
**功能**: 标签页管理器

**作用**:
- 管理已连接的标签页信息
- 记录标签页的激活、更新、关闭事件

**关键数据结构**:
- `tabs`: 标签页映射（Map<tabId, TabInfo>）
- `TabInfo`: 标签页信息对象
  - `tabId`: 标签页ID
  - `tabIndex`: 标签页索引
  - `connectAt`: 连接时间
  - `lastPingAt`: 最后心跳时间
  - `url`: 标签页URL

**主要方法**:
- `RecordActivatedTab()`: 记录激活的标签页
- `RemoveActivatedTab()`: 移除标签页
- `GetAllTabs()`: 获取所有标签页
- `GetTabInfoByTabId()`: 根据ID获取标签页信息

---

### 6. 类型定义 (Types)

#### 6.1 `src/types/index.ts`
**功能**: 类型定义文件

**作用**:
- 定义所有 TypeScript 类型和接口
- 导出常量

**关键数据结构**:
- `NodeProfile`: 节点配置接口
- `TabInfo`: 标签页信息接口
- `ElementData`: 元素数据接口
- `InstructionResult`: 指令结果接口
- `BaseInstruction`: 基础指令接口
- `FindElementInstruction`: 查找元素指令接口
- `InputInstruction`: 输入指令接口
- `KeyboardInstruction`: 键盘指令接口
- `MouseInstruction`: 鼠标指令接口
- `GetAttributeInstruction`: 获取属性指令接口
- `NavigateInstruction`: 导航指令接口
- `ScreenshotInstruction`: 截图指令接口
- `ExecuteScriptInstruction`: 执行脚本指令接口
- `Instruction`: 指令联合类型
- `ContentScriptMessageType`: 内容脚本消息类型
- `BackgroundScriptMessageType`: 背景脚本消息类型
- `WSMessage`: WebSocket 消息类型
- `ExecutorStatus`: 执行器状态接口
- `ElementTag`: 元素标记常量

---

### 7. 工具函数 (Utils)

#### 7.1 `src/utils/index.ts`
**功能**: 工具函数集合

**作用**:
- 提供通用的工具函数
- 封装浏览器 API 调用

**主要函数**:
- `GenerateUUID()`: 生成 UUID
- `GenerateRandomString()`: 生成随机字符串
- `EscapeCSSSelector()`: 转义 CSS 选择器
- `SendMessageToContentScript()`: 发送消息到内容脚本
- `SendMessageToBackgroundScript()`: 发送消息到背景脚本
- `ExecuteCDPCommand()`: 执行 CDP 命令
- `EnsureCDPConnected()`: 确保 CDP 已连接
- `DisconnectCDP()`: 断开 CDP 连接

---

### 8. 示例文件

#### 8.1 `src/example.ts`
**功能**: 示例指令定义

**作用**:
- 提供各种指令类型的示例
- 用于测试和演示

**关键数据结构**:
- `example`: 示例指令数组，包含各种类型的指令示例

---

### 9. 配置文件

#### 9.1 `package.json`
**功能**: 项目依赖和脚本配置

**关键信息**:
- 项目名称: `web-crawler-extension`
- 主要依赖: `vue`, `wxt`, `eventsource`, `node-fetch`
- 开发依赖: `typescript`, `vue-tsc`, `@wxt-dev/module-vue`
- 脚本命令:
  - `dev`: 开发模式（Chrome）
  - `build`: 构建扩展
  - `zip`: 打包扩展

#### 9.2 `wxt.config.ts`
**功能**: WXT 框架配置文件

**关键配置**:
- `entrypointsDir`: 入口文件目录
- `modules`: Vue 模块
- `manifest`: 扩展清单配置
  - `permissions`: 权限列表（activeTab, tabs, storage, scripting, alarms, debugger）
  - `host_permissions`: 主机权限（<all_urls>）
- `webExt`: Web 扩展配置
  - `chromiumArgs`: Chrome 启动参数（--remote-debugging-port=9222）

#### 9.3 `tsconfig.json`
**功能**: TypeScript 配置文件

**关键配置**:
- 继承 WXT 的 TypeScript 配置

---

### 10. 服务器文件 (Server)

#### 10.1 `server/server.js`
**功能**: WebSocket 服务器

**作用**:
- 接收客户端连接
- 处理登录认证
- 接收和处理指令、结果、CDP 响应等消息
- 保存结果和错误到文件

**关键数据结构**:
- `clients`: 客户端映射（Map<WebSocket, ClientInfo>）
- `PORT`: 服务器端口（默认 8080）
- `HOST`: 服务器主机（默认 '0.0.0.0'）

**主要功能函数**:
- `handleLogin()`: 处理登录消息
- `handleHeartbeat()`: 处理心跳消息
- `handleInstructions()`: 处理指令消息
- `handleResult()`: 处理结果消息
- `handleCDP()`: 处理 CDP 消息
- `handleTabs()`: 处理标签页消息

#### 10.2 `server/storage.js`
**功能**: 文件存储工具

**作用**:
- 保存执行结果到文件
- 保存错误信息到文件

**主要函数**:
- `saveResults()`: 保存结果到文件
- `saveError()`: 保存错误到文件

---

## 项目功能特性

### 1. 浏览器自动化控制
- **CDP 协议支持**: 通过 Chrome DevTools Protocol 完全控制浏览器
- **元素查找**: 支持 CSS、XPath、ID 三种选择器，支持相对关系筛选
- **鼠标操作**: 支持点击、双击、右键、悬停、移动等操作，支持鼠标轨迹模拟
- **键盘操作**: 支持按键按下、释放、输入等操作，支持特殊按键
- **文本输入**: 支持逐字符输入，支持中文字符和 Unicode
- **页面导航**: 支持页面跳转和加载等待
- **页面截图**: 支持 PNG 和 JPEG 格式截图

### 2. 指令执行系统
- **FIFO 队列**: 指令按创建时间顺序执行
- **重试机制**: 支持指令执行失败后自动重试
- **延迟控制**: 支持指令执行前后的延迟
- **错误处理**: 支持错误捕获和忽略
- **执行控制**: 支持暂停、恢复、停止执行
- **执行统计**: 统计执行数量、成功数、失败数

### 3. WebSocket 通信
- **双向通信**: 支持客户端和服务器双向通信
- **自动重连**: 连接断开后自动重连
- **心跳机制**: 定期发送心跳保持连接
- **消息类型**: 支持登录、心跳、指令、结果、CDP、标签页等多种消息类型

### 4. 日志收集
- **控制台日志**: 收集页面的控制台输出
- **网络日志**: 收集网络请求和响应信息
- **执行日志**: 记录指令执行结果

### 5. 用户界面
- **Popup 窗口**: 提供友好的用户界面
- **节点配置**: 管理节点信息
- **指令配置**: 输入和管理指令集
- **执行控制**: 控制指令执行流程
- **日志查看**: 查看执行日志和结果

### 6. 反检测机制
- **隐藏 webdriver**: 隐藏 `navigator.webdriver` 属性，防止网站检测
- **真实轨迹模拟**: 模拟真实的鼠标移动轨迹

---

## 项目结构

```
autojs/
├── src/
│   ├── entrypoints/          # 入口文件
│   │   ├── background.ts      # Background Script
│   │   ├── content.ts         # Content Script
│   │   ├── popup.html         # Popup HTML
│   │   └── popup/             # Popup 组件
│   │       ├── main.ts        # Popup 入口
│   │       ├── App.vue        # Popup 主组件
│   │       └── components/    # Popup 子组件
│   │           ├── NodeConfig.vue
│   │           ├── InstructionConfig.vue
│   │           ├── ExecutionControl.vue
│   │           └── ExecutionLog.vue
│   ├── executor/              # 执行器模块
│   │   ├── index.ts
│   │   ├── InstructionExecutor.ts
│   │   ├── CdpExecutor.ts
│   │   └── WebSocketConnector.ts
│   ├── instructions/          # 指令模块
│   │   ├── index.ts
│   │   ├── BaseInstruction.ts
│   │   ├── FindElementInstruction.ts
│   │   ├── InputInstruction.ts
│   │   ├── KeyboardInstruction.ts
│   │   ├── MouseInstruction.ts
│   │   ├── GetAttributeInstruction.ts
│   │   ├── NavigateInstruction.ts
│   │   ├── ScreenshotInstruction.ts
│   │   └── ExecuteScriptInstruction.ts
│   ├── managers/              # 管理器模块
│   │   ├── index.ts
│   │   ├── ElementManager.ts
│   │   ├── InstructionManager.ts
│   │   ├── InstructionResultManager.ts
│   │   ├── NodeConfig.ts
│   │   └── TabManager.ts
│   ├── types/                 # 类型定义
│   │   └── index.ts
│   ├── utils/                 # 工具函数
│   │   └── index.ts
│   └── example.ts             # 示例文件
├── server/                    # WebSocket 服务器
│   ├── server.js
│   ├── storage.js
│   └── package.json
├── package.json               # 项目配置
├── tsconfig.json             # TypeScript 配置
├── wxt.config.ts            # WXT 配置
└── 项目文档.md              # 本文档
```

---

## 安装和运行

### 1. 环境要求
- Node.js >= 16.0.0
- npm 或 yarn
- Chrome 浏览器（推荐最新版本）

### 2. 安装依赖

```bash
# 安装扩展依赖
npm install

# 安装服务器依赖
cd server
npm install
cd ..
```

### 3. 开发模式运行

```bash
# 启动扩展开发模式（会自动打开 Chrome 浏览器）
npm run dev

# 启动 WebSocket 服务器（需要新开一个终端）
cd server
npm start
```

### 4. 构建扩展

```bash
# 构建 Chrome 扩展
npm run build

# 打包扩展（生成 .zip 文件）
npm run zip
```

### 5. 手动安装扩展

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `output/chrome-mv3` 目录（构建后）或 `.output/chrome-mv3` 目录（开发模式）

### 6. 配置节点信息

1. 点击扩展图标打开 Popup 窗口
2. 在"节点配置"标签页中配置节点名称和令牌
3. 点击"保存"按钮

### 7. 连接 WebSocket 服务器

1. 在"执行控制"标签页中
2. 输入 WebSocket URL（默认：`ws://localhost:8080`）
3. 点击"测试连接"测试连接
4. 点击"连接"建立连接

### 8. 执行指令

1. 在"指令配置"标签页中
2. 选择目标标签页
3. 输入指令集 JSON（或点击"加载示例"）
4. 点击"发送指令"
5. 在"执行控制"标签页中点击"开始执行"

---

## 注意事项

### 1. 权限要求
- 扩展需要以下权限：
  - `activeTab`: 访问当前活动标签页
  - `tabs`: 访问所有标签页
  - `storage`: 本地存储
  - `scripting`: 脚本注入
  - `alarms`: 定时任务
  - `debugger`: CDP 调试（必需）
  - `<all_urls>`: 访问所有网站

### 2. CDP 连接
- 扩展使用 `browser.debugger` API 连接 CDP
- 如果其他扩展或 DevTools 已连接，可能会产生冲突
- 建议关闭其他可能使用 CDP 的扩展

### 3. 元素查找
- 元素查找使用 CDP DOM 域，需要先启用 `DOM.enable`
- 找到的元素会设置一个唯一的标记（tag），用于后续操作
- 如果多个元素匹配，会优先选择可见的元素
- 支持通过相对关系（父元素、子元素、兄弟元素）进一步筛选

### 4. 指令执行
- 指令按创建时间（`created_at`）排序执行（FIFO 队列）
- 每个指令可以设置延迟（`delay`）、重试次数（`retry`）、超时时间（`timeout`）
- 指令执行失败会根据 `retry` 设置自动重试
- 如果设置了 `ignoreError`，即使失败也会继续执行下一条指令

### 5. WebSocket 通信
- WebSocket 连接需要先登录（发送节点配置）
- 连接断开后会自动重连（5秒间隔）
- 心跳间隔为 30 秒
- 消息大小限制为 10MB

### 6. 鼠标轨迹模拟
- `calculated`: 使用贝塞尔曲线模拟轨迹（默认）
- `simulated`: 使用更复杂的算法模拟真实轨迹
- `none`: 不模拟轨迹，直接移动到目标位置

### 7. 文本输入
- 文本输入使用 CDP `Input.dispatchKeyEvent` 的 `char` 类型事件
- 支持中文字符和 Unicode 字符（包括 emoji）
- 可以设置 `delay` 来控制每个字符之间的输入间隔

### 8. 反检测
- Content Script 在 `document_start` 阶段运行，尽早隐藏 `webdriver` 属性
- 使用真实的鼠标轨迹模拟，减少被检测的风险
- 但完全避免检测是不可能的，某些网站仍可能检测到自动化工具

### 9. 性能考虑
- 每个标签页最多保存 10000 条控制台日志和网络日志
- 指令执行是同步的，大量指令可能需要较长时间
- 建议合理设置指令的延迟和重试次数

### 10. 错误处理
- 所有错误都会被捕获并记录到执行结果中
- 可以通过 `ignoreError` 选项忽略某些错误
- 执行日志会显示所有成功和失败的指令

### 11. 服务器配置
- WebSocket 服务器默认运行在 `localhost:8080`
- 可以通过环境变量 `PORT` 和 `HOST` 修改端口和主机
- 结果和错误会保存到 `server/data/` 目录

### 12. 开发建议
- 使用 TypeScript 的类型检查确保代码质量
- 遵循项目的代码结构和命名规范
- 添加新指令类型时，需要在 `InstructionFactory` 中注册
- 测试时可以使用 `example.ts` 中的示例指令

---

## 总结

AutoJS 是一个功能完整的 Web 自动化爬虫 Chrome 扩展，通过 CDP 协议实现浏览器控制，支持丰富的操作指令，提供友好的用户界面，并通过 WebSocket 与服务器通信。项目采用模块化设计，代码结构清晰，易于扩展和维护。
