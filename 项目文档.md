# AutoJS 项目文档

## 目录

1. [项目概述](#项目概述)
2. [架构设计](#架构设计)
3. [源代码文件详细说明](#源代码文件详细说明)
4. [项目功能特性](#项目功能特性)
5. [项目结构](#项目结构)
6. [安装和运行](#安装和运行)
7. [配置说明](#配置说明)
8. [注意事项](#注意事项)
9. [API 接口说明](#api-接口说明)

---

## 项目概述

AutoJS 是一个基于 **WXT + Vue 3 + TypeScript** 的 Chrome 浏览器扩展，用于实现 Web 自动化爬虫功能。项目通过 **Chrome DevTools Protocol (CDP)** 协议控制浏览器，支持元素查找、鼠标键盘操作、页面导航、截图等功能，并通过 **WebSocket** 与服务器进行双向通信。

### 核心特性

- **浏览器自动化控制**: 通过 CDP 协议完全控制浏览器行为
- **指令执行系统**: FIFO 队列执行，支持重试、延迟、错误处理
- **WebSocket 通信**: 双向通信，自动重连，心跳保活
- **日志收集系统**: 控制台日志、网络日志收集和管理
- **HTTP 请求执行**: 支持通过 WebSocket 执行 HTTP 请求
- **侧边栏界面**: 提供友好的用户界面（Side Panel）
- **Native Messaging 日志**: 通过本地应用写入日志文件
- **Service Worker 生命周期管理**: 自动处理休眠和唤醒

---

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Chrome Extension                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   Popup UI   │    │  Side Panel  │    │Content Script│  │
│  │   (Vue 3)    │    │   (Vue 3)    │    │  (DOM操作)   │  │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘  │
│         │                    │                    │          │
│         └────────────────────┼────────────────────┘          │
│                              │                                │
│                    ┌─────────▼─────────┐                     │
│                    │  Background Script│                     │
│                    │   (Service Worker)│                     │
│                    └─────────┬─────────┘                     │
│                              │                                │
│         ┌────────────────────┼────────────────────┐         │
│         │                    │                    │         │
│  ┌──────▼──────┐   ┌────────▼────────┐  ┌───────▼──────┐ │
│  │ Instruction │   │   CDP Executor  │  │ HTTP Executor│ │
│  │  Executor   │   │                 │  │              │ │
│  └──────┬──────┘   └────────┬─────────┘  └───────┬──────┘ │
│         │                   │                    │         │
│  ┌──────▼──────┐   ┌────────▼────────┐           │         │
│  │ Instruction │   │  WebSocket     │           │         │
│  │  Manager    │   │  Connector     │◄──────────┘         │
│  └─────────────┘   └────────┬────────┘                     │
│                              │                                │
└──────────────────────────────┼────────────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   WebSocket Server  │
                    │   (Go/Node.js)      │
                    └─────────────────────┘
```

### 核心模块说明

1. **Background Script (Service Worker)**

   - 扩展的核心后台服务
   - 管理所有执行器和连接器
   - 处理消息路由和生命周期事件
   - 监听 CDP 事件（控制台、网络）

2. **Content Script**

   - 在页面上下文中运行
   - 提供 DOM 操作和元素查找
   - 隐藏 `navigator.webdriver` 属性

3. **Instruction Executor**

   - 指令执行引擎
   - FIFO 队列管理
   - 执行状态控制（运行、暂停、停止）

4. **CDP Executor**

   - CDP 命令执行
   - 日志收集（控制台、网络）
   - 标签页管理

5. **HTTP Executor**

   - HTTP 请求执行
   - 支持所有 HTTP 方法
   - 超时和错误处理

6. **WebSocket Connector**
   - WebSocket 连接管理
   - 自动重连机制
   - 心跳保活
   - 消息路由

---

## 源代码文件详细说明

### 1. 入口文件 (Entrypoints)

#### 1.1 `src/entrypoints/background.ts`

**功能**: Background Script 入口文件，扩展的后台服务核心（Service Worker）

**作用**:

- 初始化所有执行器（InstructionExecutor、CdpExecutor、HttpExecutor）
- 初始化 WebSocket 连接器和服务（WebSocketService、CdpEventService、InitializationService）
- 处理来自 popup 和 content script 的消息
- 管理 Service Worker 生命周期（休眠、唤醒、重连）
- 通过服务模块化组织代码，提高可维护性

**关键数据结构**:

- `mapTypeToFunction`: 消息类型到处理函数的映射
- `instructionExecutor`: 指令执行器实例
- `cdpExecutor`: CDP 执行器实例
- `httpExecutor`: HTTP 执行器实例
- `wsConnector`: WebSocket 连接器实例
- `webSocketService`: WebSocket 服务实例
- `cdpEventService`: CDP 事件服务实例
- `initializationService`: 初始化服务实例

**模块化结构**:

- **Handlers** (`src/entrypoints/background/handlers/`): 消息处理器模块
  - `tabs.ts`: 标签页相关消息处理
  - `node.ts`: 节点配置相关消息处理
  - `instructions.ts`: 指令相关消息处理（添加、执行、暂停、停止等）
  - `websocket.ts`: WebSocket 相关消息处理（连接、断开、测试等）
- **Services** (`src/entrypoints/background/services/`): 后台服务模块
  - `websocket.ts`: WebSocket 服务（管理连接、设置回调）
  - `cdp.ts`: CDP 事件服务（监听控制台日志、网络日志）
  - `initialization.ts`: 初始化服务（创建定时任务、处理生命周期）

**主要功能函数**（通过 handlers 模块提供）:

- `get_tabs()`: 获取所有标签页列表
- `get_node_profile()`: 获取节点配置
- `update_node_profile()`: 更新节点配置
- `add_instructions()`: 添加指令集（从 JSON 字符串解析）
- `execute_instructions()`: 执行指令集（异步执行）
- `pause_execution()`: 暂停执行
- `stop_execution()`: 停止执行
- `get_executor_status()`: 获取执行器状态
- `get_results()`: 获取执行结果
- `clear_results()`: 清空执行结果
- `connect_websocket()`: 连接 WebSocket
- `disconnect_websocket()`: 断开 WebSocket
- `test_websocket()`: 测试 WebSocket 连接
- `send_results_to_server()`: 发送执行结果到服务器
- `content_script_loaded()`: 处理内容脚本加载完成消息
- `setCallbacks()`: 设置 WebSocket 消息处理器回调

**生命周期事件处理**:

- `browser.runtime.onStartup`: Chrome 启动时初始化（调用 InitializationService）
- `browser.runtime.onInstalled`: 扩展安装/更新时初始化（调用 InitializationService）
- `browser.runtime.onSuspend`: Service Worker 休眠时记录警告
- `browser.alarms.onAlarm`: 定时任务触发（通过 InitializationService 处理 WebSocket 重连检查）
- `browser.debugger.onEvent`: CDP 事件监听（通过 CdpEventService 处理控制台、网络日志）

**消息类型映射**:

- `get_tabs`: 获取标签页列表
- `get_node_profile`: 获取节点配置
- `update_node_profile`: 更新节点配置
- `add_instructions`: 添加指令
- `execute_instructions`: 执行指令
- `pause_execution`: 暂停执行
- `stop_execution`: 停止执行
- `get_executor_status`: 获取执行器状态
- `get_results`: 获取结果
- `clear_results`: 清空结果
- `connect_websocket`: 连接 WebSocket
- `disconnect_websocket`: 断开 WebSocket
- `test_websocket`: 测试 WebSocket
- `send_results_to_server`: 发送结果到服务器
- `content_script_loaded`: 内容脚本加载完成
- `setCallbacks`: 设置回调

#### 1.2 `src/entrypoints/content.ts`

**功能**: Content Script 入口文件，在页面上下文中运行

**作用**:

- 隐藏 `navigator.webdriver` 属性，防止网站检测自动化工具
- 提供 DOM 元素查找功能（CSS、XPath、ID、Tag 选择器）
- 提供元素属性获取功能（支持标准属性、计算样式、图片等）
- 提供元素可见性检查功能（全面的可见性检查）
- 提供元素滚动到视图功能
- 提供元素文本获取功能

**运行时机**: `runAt: 'document_start'` - 在文档加载前运行，确保尽早隐藏 webdriver 属性

**关键数据结构**:

- `mapTypeToFunction`: 消息类型到处理函数的映射
- `ElementTag`: 元素标记常量 `'cdp-locate-id'`（从 consts 导入）

**主要功能函数**:

- `hideWebdriver()`: 隐藏 webdriver 属性（使用 Object.defineProperty）
- `FindElement(selector, selectorType)`: 查找元素（支持 CSS、XPath、ID、Tag、Text、Ledby）
- `FindElementByTag(tag)`: 通过标记查找元素
- `GetAttribute(message)`: 获取元素属性（支持标准属性、计算样式、图片）
- `GetText(message)`: 获取元素文本
- `IsVisible(element)`: 检查元素是否可见（全面的可见性检查）
- `CheckElementVisible(message)`: 检查元素可见性的消息处理函数
- `ScrollIntoView(message)`: 滚动元素到视图（平滑滚动，居中显示）
- `checkParentVisibility(element)`: 递归检查父元素链是否可见

**消息类型映射**:

- `scroll_into_view`: 滚动元素到视图
- `get_attribute`: 获取元素属性
- `get_text`: 获取元素文本
- `is_visible`: 检查元素是否可见
- `execute_script`: 执行脚本（当前未实现，返回 `Not implemented` 错误）

**可见性检查标准**（13 个维度）:

1. 元素是否已连接到 DOM (`isConnected`)
2. 能否获取计算样式（`getComputedStyle`）
3. CSS `display` 属性是否为 `none`
4. CSS `visibility` 属性是否为 `hidden` 或 `collapse`
5. CSS `opacity` 属性是否 <= 0
6. 父元素链的可见性（递归检查 `display`、`visibility`、`opacity`）
7. CSS `transform` 属性是否包含 `scale(0)`、`scaleX(0)` 或 `scaleY(0)`
8. 能否获取元素的边界框（`getBoundingClientRect`）
9. 元素边界框尺寸是否有效 (`width > 0 && height > 0`)
10. 元素是否在视口内（允许 50px 边距，处理部分可见的元素）
11. CSS `clip` 属性是否完全裁剪（已废弃但为兼容性保留）
12. CSS `clip-path` 属性是否完全裁剪（现代方法）
13. `offsetWidth` 和 `offsetHeight` 是否为 0（辅助检查，考虑内联元素特殊情况）

#### 1.3 `src/entrypoints/popup/main.ts`

**功能**: Popup 窗口入口文件

**作用**:

- 初始化 Vue 3 应用
- 挂载 App.vue 组件到页面

**关键数据结构**: 无

**实现方式**: 使用 Vue 3 的 `createApp` API 创建应用实例并挂载到 `#app` 元素

#### 1.4 `src/entrypoints/popup/App.vue`

**功能**: Popup 窗口主组件

**作用**:

- 提供标签页导航界面
- 集成四个主要功能模块：节点配置、指令配置、执行控制、执行日志

**关键数据结构**:

- `activeTab`: 当前激活的标签页 ID
- `tabs`: 标签页配置数组

**组件**:

- `NodeConfig`: 节点配置组件
- `InstructionConfig`: 指令配置组件
- `ExecutionControl`: 执行控制组件
- `ExecutionLog`: 执行日志组件

---

### 2. Popup 组件 (Popup Components)

#### 2.1 `src/entrypoints/popup/components/NodeConfig.vue`

**功能**: 节点配置管理组件

**作用**:

- 显示和编辑节点配置信息（节点类型、ID、名称、令牌）
- 从 background script 加载配置
- 保存配置到 background script

**关键数据结构**:

- `profile`: 节点配置对象（NodeProfile 类型）
  - `node_type`: 节点类型（固定为 'crawler'）
  - `node_id`: 节点 ID（自动生成 UUID，不可修改）
  - `node_name`: 节点名称（可编辑）
  - `node_token`: 节点令牌（可编辑）

#### 2.2 `src/entrypoints/popup/components/InstructionConfig.vue`

**功能**: 指令配置管理组件

**作用**:

- 选择目标标签页
- 输入和验证指令集 JSON
- 发送指令到 background script
- 加载示例指令

**关键数据结构**:

- `selectedTabId`: 选中的标签页 ID
- `instructionsJson`: 指令集 JSON 字符串
- `tabs`: 标签页列表

#### 2.3 `src/entrypoints/popup/components/ExecutionControl.vue`

**功能**: 执行控制组件

**作用**:

- 管理 WebSocket 连接（连接、断开、测试）
- 控制指令执行（开始、暂停、停止）
- 显示执行状态（运行状态、暂停状态、执行统计）

**关键数据结构**:

- `wsUrl`: WebSocket URL
- `isConnected`: WebSocket 连接状态
- `selectedTabId`: 选中的标签页 ID
- `executorStatus`: 执行器状态对象
  - `isRunning`: 是否正在运行
  - `isPaused`: 是否暂停
  - `executedCount`: 已执行数量
  - `successCount`: 成功数量
  - `errorCount`: 失败数量
  - `startTime`: 开始时间

#### 2.4 `src/entrypoints/popup/components/ExecutionLog.vue`

**功能**: 执行日志查看组件

**作用**:

- 显示指令执行结果日志
- 支持过滤（仅显示成功/失败）
- 清空日志
- 发送日志到服务器

**关键数据结构**:

- `logs`: 指令结果数组（InstructionResult[]）
- `filterSuccess`: 是否仅显示成功
- `filterError`: 是否仅显示失败

---

### 3. 执行器模块 (Executor)

#### 3.1 `src/executor/InstructionExecutor.ts`

**功能**: 指令执行器核心类

**作用**:

- 管理指令执行流程（FIFO 队列）
- 控制执行状态（运行、暂停、停止）
- 统计执行结果（成功数、失败数）
- 处理 WebSocket 消息中的指令
- 按标签页分组执行指令

**关键数据结构**:

- `instructionManager`: 指令管理器实例
- `resultManager`: 结果管理器实例
- `isRunning`: 是否正在运行
- `isPaused`: 是否暂停
- `stopRequested`: 是否请求停止
- `executedCount`: 已执行数量
- `successCount`: 成功数量
- `errorCount`: 失败数量
- `startTime`: 开始时间
- `sendResult`: 发送结果的回调函数

**主要方法**:

- `ExecuteAll(instructions)`: 执行所有指令
  - 将指令添加到指令管理器
  - 初始化执行器状态
  - 执行指令循环（最多 10 轮，每轮处理所有标签页）
  - 对每个标签页：
    - 检查标签页是否存在
    - 确保 CDP 已连接
    - 启用必要的 CDP 域（DOM、CSS、Page、Runtime）
    - 循环执行该标签页的所有指令（FIFO）
    - 执行完成后发送结果到服务器
  - 处理错误和异常
  - 重置执行状态
- `Pause()`: 暂停执行
- `Resume()`: 恢复执行
- `Stop()`: 停止执行（清空结果）
- `GetStatus()`: 获取执行状态
- `IsExecuting()`: 是否正在执行
- `handleMessage(message)`: 处理 WebSocket 消息中的指令
  - 解析消息数据为指令数组
  - 使用 InstructionFactory 创建指令实例
  - 调用 ExecuteAll 执行指令
- `setSendResult(callback)`: 设置发送结果的回调函数
- `GetInstructionManager()`: 获取指令管理器
- `GetResultManager()`: 获取结果管理器
- `Delay(time)`: 等待指定时间（用于暂停检查循环）

**执行流程**:

1. 接收指令列表（通过 `add_instructions` 或 WebSocket 消息）
2. 指令按 `created_at` 排序（FIFO）
3. 按标签页分组
4. 循环执行（最多 10 轮）：
   - 检查停止请求
   - 检查暂停状态
   - 遍历所有有指令的标签页
   - 对每个标签页执行所有待执行指令
5. 执行完成后发送结果到服务器

#### 3.2 `src/executor/CdpExecutor.ts`

**功能**: CDP (Chrome DevTools Protocol) 执行器

**作用**:

- 执行 CDP 命令（列出标签页、元素截图、执行 JavaScript、源码搜索等）
- 收集和管理控制台日志
- 收集和管理网络日志
- 处理 WebSocket 消息中的 CDP 请求
- 创建新标签页并导航

**关键数据结构**:

- `consoleLogs`: 控制台日志映射（Map<logKey, log[]>），logKey 格式：`console_${tabId}`
- `networkLogs`: 网络日志映射（Map<logKey, log[]>），logKey 格式：`network_${tabId}`
- `MAX_LOG_ENTRIES`: 每个标签页最多保存的日志条目数（10000）
- `mapTypeToFunction`: CDP 消息类型到处理函数的映射
- `sendResult`: 发送结果的回调函数

**主要方法**:

- `handleMessage(cdpMessage)`: 统一的 WebSocket 消息处理接口
  - 根据消息类型分发到相应的处理函数
  - 处理错误并返回错误结果
- `handleCdpConnect()`: 连接 CDP 到指定标签页
- `handleCdpDisconnect()`: 断开 CDP 连接
- `handleListTargets()`: 列出所有标签页
- `handleCreateTabAndNavigate()`: 创建新标签页并导航到指定 URL
  - 创建标签页
  - 等待页面加载完成（最多 30 秒）
  - 返回标签页信息
- `handleExecuteJavaScript()`: 执行 JavaScript 代码
  - 启用 Runtime 域
  - 执行 Runtime.evaluate
  - 返回执行结果和异常信息
- `handleTakeElementScreenshot()`: 截取元素截图
  - 启用 DOM 和 Page 域
  - 获取文档根节点
  - 通过选择器查找元素（支持 CSS、XPath、ID）
  - 滚动元素到可视区
  - 获取元素盒模型（位置和尺寸）
  - 计算元素在文档中的绝对坐标
  - 截取元素截图（base64 编码）
- `handleSendCommand()`: 执行通用 CDP 命令
  - 确保 CDP 已连接
  - 执行指定的 CDP 方法
  - 返回执行结果
- `handleGrepSource()`: 源码搜索
  - 启用 Page 域
  - 获取页面资源树
  - 递归遍历资源树
  - 获取每个资源的内容
  - 使用正则表达式搜索匹配内容
  - 返回匹配结果（URL、行号、内容）
- `handleGetNetworkLogs()`: 获取网络日志
  - 支持按 requestId 过滤
  - 支持按条件过滤（事件类型、URL、方法、状态码、时间范围）
  - 支持按请求分组
  - 支持分页（limit、offset）
  - 支持清空日志
- `handleGetConsoleLogs()`: 获取控制台日志
  - 支持按条件过滤（日志类型、级别、文本、时间范围）
  - 支持分页（limit、offset）
  - 支持清空日志
- `handleInitNetworkLogs()`: 初始化网络日志收集
  - 确保 CDP 已连接
  - 启用 Network 域
  - 可选清空现有日志
- `handleInitConsoleLogs()`: 初始化控制台日志收集
  - 确保 CDP 已连接
  - 启用 Runtime 域（控制台日志通过 Runtime.consoleAPICalled 事件收集）
  - 可选清空现有日志
- `handleCloseNetworkLogs()`: 关闭网络日志收集
  - 禁用 Network 域
  - 可选清空日志
- `handleCloseConsoleLogs()`: 关闭控制台日志收集
  - 禁用 Runtime 域
  - 可选清空日志
- `addConsoleLog(tabId, logEntry)`: 添加控制台日志
  - 转换时间戳（CDP 时间戳转换为毫秒时间戳）
  - 添加收集时间
  - 限制日志数量（超过 MAX_LOG_ENTRIES 时移除最旧的）
- `addNetworkLog(tabId, logEntry)`: 添加网络日志
  - 转换时间戳
  - 添加收集时间
  - 限制日志数量
- `clearConsoleLogs(tabId)`: 清空指定标签页的控制台日志
- `clearNetworkLogs(tabId)`: 清空指定标签页的网络日志
- `filterNetworkLogs(logs, filter)`: 过滤网络日志
  - 按事件类型过滤
  - 按 URL 过滤
  - 按请求方法过滤
  - 按状态码过滤
  - 按时间范围过滤
- `filterConsoleLogs(logs, filter)`: 过滤控制台日志
  - 按日志类型过滤
  - 按日志级别过滤
  - 按文本内容过滤
  - 按时间范围过滤
- `groupNetworkLogsByRequest(logs)`: 按请求 ID 分组网络日志
  - 将同一 requestId 的日志合并为一个对象
  - 包含完整的请求生命周期（requestWillBeSent、responseReceived、loadingFinished、loadingFailed）
- `setSendResult(callback)`: 设置发送结果的回调函数

**CDP 消息类型**:

- `cdp_connect`: 连接 CDP
- `cdp_disconnect`: 断开 CDP
- `list_targets`: 列出所有标签页
- `create_tab_and_navigate`: 创建新标签页并导航
- `execute_javascript`: 执行 JavaScript
- `take_element_screenshot`: 截取元素截图
- `send_command`: 执行通用 CDP 命令
- `grep_source`: 源码搜索
- `get_network_logs`: 获取网络日志
- `get_console_logs`: 获取控制台日志
- `init_network_logs`: 初始化网络日志收集
- `init_console_logs`: 初始化控制台日志收集
- `close_network_logs`: 关闭网络日志收集
- `close_console_logs`: 关闭控制台日志收集

#### 3.3 `src/executor/HttpExecutor.ts`

**功能**: HTTP 请求执行器

**作用**:

- 执行 HTTP 请求（GET、POST、PUT、DELETE 等）
- 处理请求和响应
- 支持超时控制
- 处理错误和异常

**关键数据结构**:

- `mapTypeToFunction`: HTTP 消息类型到处理函数的映射
- `sendResult`: 发送结果的回调函数

**主要方法**:

- `handleMessage(httpMessage)`: 统一的 WebSocket 消息处理接口
  - 根据消息类型分发到相应的处理函数
  - 处理错误并返回错误结果
- `handleHttpRequest()`: 执行 HTTP 请求
  - 验证请求参数（method、url）
  - 准备请求选项（headers、body）
  - 处理请求体（字符串或对象，自动转换为 JSON）
  - 设置 Content-Type（如果是对象且未指定）
  - 创建超时 Promise（默认 180 秒）
  - 使用 fetch API 执行请求
  - 获取响应状态、响应头、响应体
  - 解析响应体（JSON 或文本）
  - 返回响应结果
- `setSendResult(callback)`: 设置发送结果的回调函数

**HTTP 消息类型**:

- `http_request`: 执行 HTTP 请求

**支持的 HTTP 方法**:

- GET、POST、PUT、DELETE、PATCH、HEAD、OPTIONS

**请求参数**:

- `method`: HTTP 方法（必需）
- `url`: 请求 URL（必需）
- `headers`: 请求头（可选）
- `body`: 请求体（可选，字符串或对象）
- `timeout`: 超时时间（秒，可选，默认 180 秒）

**响应格式**:

- `status`: HTTP 状态码
- `statusText`: HTTP 状态文本
- `headers`: 响应头对象
- `body`: 响应体（JSON 对象或字符串）
- `url`: 最终请求 URL（可能被重定向）

#### 3.4 `src/executor/WebSocketConnector.ts`

**功能**: WebSocket 连接器

**作用**:

- 管理与服务器的 WebSocket 连接
- 处理连接、重连、心跳
- 发送和接收消息
- 处理登录认证
- 消息类型路由

**关键数据结构**:

- `ws`: WebSocket 实例
- `url`: WebSocket URL
- `reconnectInterval`: 重连间隔（5 秒）
- `heartbeatInterval`: 心跳间隔（15 秒）
- `connected`: 连接状态
- `isLoggedIn`: 登录状态
- `isDisconnecting`: 是否正在断开连接
- `reconnectTimer`: 重连定时器
- `heartbeatTimer`: 心跳定时器
- `mapMessageTypeToFunction`: 消息类型到处理函数的映射

**主要方法**:

- `connect()`: 连接 WebSocket
  - 检查现有连接状态
  - 清理旧连接
  - 创建新 WebSocket 连接
  - 设置事件处理器（onopen、onmessage、onerror、onclose）
  - 连接成功后发送登录消息
  - 启动心跳
- `disconnect()`: 主动断开连接
  - 设置断开标记
  - 清除重连定时器
  - 停止心跳
  - 清理 WebSocket 连接
- `sendMessage(message)`: 发送消息
  - 检查连接状态
  - 检查消息大小（最大 10MB）
  - 序列化为 JSON 字符串
  - 发送消息
- `testConnection(url, timeout)`: 测试连接
  - 验证 URL 格式
  - 创建测试连接
  - 设置超时（默认 5 秒）
  - 返回连接是否成功
- `registerMessageTypeHandler(type, func)`: 注册消息类型处理器
- `sendLoginMessage()`: 发送登录消息
  - 获取节点配置
  - 构造登录消息
  - 发送消息
- `sendHeartbeat()`: 发送心跳消息
- `handleMessage(event)`: 处理接收到的消息
  - 解析 JSON 消息
  - 处理登录响应
  - 处理心跳响应
  - 处理错误消息
  - 路由到注册的处理器
- `handleLoginResponse(message)`: 处理登录响应
  - 更新登录状态
  - 登录失败时断开连接
- `handleHeartbeatResponse(message)`: 处理心跳响应
- `handleErrorMessage(message)`: 处理错误消息
- `isWebSocketOpen()`: 检查 WebSocket 是否打开
- `isConnected()`: 检查是否已连接且已登录
- `startHeartbeat()`: 开始心跳
- `stopHeartbeat()`: 停止心跳
- `scheduleReconnect()`: 安排重连
- `clearReconnectTimer()`: 清除重连定时器
- `cleanupWebSocket()`: 清理 WebSocket 连接

**消息类型**:

- `login`: 登录消息
- `heartbeat`: 心跳消息
- `error`: 错误消息
- `instructions`: 指令消息（路由到 InstructionExecutor，通过 WebSocketService 设置的回调处理）
- `cdp`: CDP 消息（路由到 CdpExecutor，通过 WebSocketService 设置的回调处理）
- `http`: HTTP 消息（路由到 HttpExecutor，通过 WebSocketService 设置的回调处理）
- `tabs`: 标签页消息

**连接流程**:

1. 调用 `connect()` 创建 WebSocket 连接
2. 连接成功后触发 `onopen` 事件
3. 发送登录消息（包含节点配置）
4. 服务器返回登录响应
5. 更新登录状态（`isLoggedIn`）
6. 启动心跳定时器（每 15 秒发送一次心跳）
7. 开始接收和处理消息（通过注册的消息类型处理器路由）

**重连机制**:

- 连接断开时（非主动断开）自动安排重连
- 重连间隔：5 秒
- 重连前会清理旧连接和定时器

**心跳机制**:

- 心跳间隔：15 秒
- 每次心跳发送时间戳
- 服务器返回心跳响应

---

### 4. 指令模块 (Instructions)

#### 4.1 `src/instructions/BaseInstruction.ts`

**功能**: 基础指令抽象类

**作用**:

- 定义所有指令的通用属性和方法
- 提供延迟、重试、CDP 命令执行等通用功能
- 作为所有具体指令类的基类

**关键数据结构**:

- `tabId`: 标签页 ID（必需）
- `type`: 指令类型（必需）
- `instructionID`: 指令 ID（必需，唯一标识）
- `delay`: 延迟时间（秒，可选）
- `retry`: 重试次数（可选，默认 0，表示不重试）
- `timeout`: 超时时间（可选）
- `ignoreError`: 是否忽略错误（可选）
- `created_at`: 创建时间（可选，用于排序）
- `params`: 额外参数（可选）

**主要方法**:

- `Execute()`: 执行指令（抽象方法，由子类实现）
- `Delay(time)`: 等待指定时间
  - 参数 `time` 单位为秒
  - 如果未提供，使用指令的 `delay` 属性
  - 转换为毫秒后等待
- `Retry(fn)`: 重试执行指令
  - 参数 `fn` 是要执行的异步函数
  - 根据 `retry` 属性决定重试次数
  - `retry = 0` 表示不重试（但仍执行一次）
  - `retry = 1` 表示最多执行 2 次（初始 + 1 次重试）
  - 每次重试前等待 1 秒
  - 返回执行结果（包含成功或失败信息）
- `ExecuteCDPCommand(method, params)`: 执行 CDP 命令
  - 使用 `ExecuteCDPCommand` 工具函数
  - 自动使用指令的 `tabId`
- `SendMessageToContentScript(message)`: 发送消息到内容脚本
  - 使用 `SendMessageToContentScript` 工具函数
  - 自动使用指令的 `tabId`
- `ToObject()`: 转换为对象（用于序列化）

#### 4.2 `src/instructions/FindElementInstruction.ts`

**功能**: 元素查找指令

**作用**:

- 根据选择器查找页面元素
- 支持 CSS、XPath、ID、Tag、Text、Ledby 六种选择器类型
- 支持相对关系筛选（父元素、子元素、兄弟元素）
- 检查元素可见性
- 为找到的元素设置标记（tag）

**关键数据结构**:

- `elementData`: 元素数据对象（ElementData 类型）
  - `name`: 元素名称（必需）
  - `selector`: 选择器（必需）
  - `selectorType`: 选择器类型（'css' | 'xpath' | 'id' | 'tag' | 'text' | 'ledby'）
  - `text`: 文本内容（用于 text 和 ledby 类型选择器）
  - `parentName`: 父元素名称（可选）
  - `childrenName`: 子元素名称（可选）
  - `siblingName`: 兄弟元素名称（可选）
  - `siblingOffset`: 兄弟元素偏移量（可选）

**主要方法**:

- `Execute()`: 执行元素查找
  - 使用 ElementManager 定位元素
  - 返回查找结果
- `FindAllMatchingElementNodeIds()`: 查找所有匹配的元素节点 ID
- `checkNodeRelations()`: 检查节点相对关系
- `checkNodeIsVisible()`: 检查节点是否可见
- `selectElementByParent()`: 根据父元素筛选
- `selectElementByChildren()`: 根据子元素筛选
- `selectElementBySibling()`: 根据兄弟元素筛选

#### 4.3 `src/instructions/InputInstruction.ts`

**功能**: 文本输入指令

**作用**:

- 向输入框输入文本
- 支持清空输入框
- 支持逐字符输入（可设置延迟）
- 支持中文字符和 Unicode 字符

**关键数据结构**:

- `elementName`: 元素名称
- `text`: 要输入的文本
- `clear`: 是否清空输入框

#### 4.4 `src/instructions/KeyboardInstruction.ts`

**功能**: 键盘操作指令

**作用**:

- 模拟键盘按键操作
- 支持 press、type、keydown、keyup 四种操作
- 支持特殊按键（Enter、Escape、方向键等）
- 支持聚焦元素

**关键数据结构**:

- `elementName`: 元素名称（可选）
- `action`: 操作类型（'press' | 'type' | 'keydown' | 'keyup'）
- `key`: 按键名称或字符

**主要方法**:

- `executePress()`: 执行按键按下操作
- `executeType()`: 执行输入操作
- `executeKeyDown()`: 执行按键按下
- `executeKeyUp()`: 执行按键释放
- `isSpecialKey()`: 判断是否为特殊按键
- `getVirtualKeyCode()`: 获取虚拟键码

#### 4.5 `src/instructions/MouseInstruction.ts`

**功能**: 鼠标操作指令

**作用**:

- 模拟鼠标操作（点击、双击、右键点击、悬停、移动等）
- 支持鼠标轨迹模拟（calculated、simulated、none）
- 支持基于元素或坐标的鼠标操作

**关键数据结构**:

- `action`: 操作类型（'click' | 'dblclick' | 'rightclick' | 'hover' | 'left_mousedown' | 'left_mouseup' | 'right_mousedown' | 'right_mouseup' | 'move_to'）
- `elementName`: 元素名称（可选）
- `x`: X 坐标（可选）
- `y`: Y 坐标（可选）
- `simulate`: 轨迹模拟类型（'calculated' | 'simulated' | 'none'）

**主要方法**:

- `MoveMouseTo()`: 移动鼠标到目标位置
- `CalculatedMouseTrajectory()`: 计算鼠标轨迹（贝塞尔曲线）
- `SimulatedMouseTrajectory()`: 模拟鼠标轨迹（更真实的算法）
- `NoneMouseTrajectory()`: 不模拟轨迹，直接移动

#### 4.6 `src/instructions/GetAttributeInstruction.ts`

**功能**: 获取元素属性指令

**作用**:

- 获取元素的属性值
- 支持标准 HTML 属性、计算样式、图片等

**关键数据结构**:

- `elementName`: 元素名称
- `attribute`: 属性名称（可选，默认为 'text'）

#### 4.7 `src/instructions/SetAttributeInstruction.ts`

**功能**: 设置元素属性指令

**作用**:

- 设置元素的属性值
- 支持标准 HTML 属性

**关键数据结构**:

- `elementName`: 元素名称
- `attribute`: 属性名称
- `value`: 属性值

#### 4.8 `src/instructions/NavigateInstruction.ts`

**功能**: 页面导航指令

**作用**:

- 导航到指定 URL
- 等待页面加载完成

**关键数据结构**:

- `url`: 目标 URL

**主要方法**:

- `WaitForPageLoad()`: 等待页面加载完成

#### 4.9 `src/instructions/ScreenshotInstruction.ts`

**功能**: 页面截图指令

**作用**:

- 截取页面截图
- 支持 PNG 和 JPEG 格式
- 支持设置图片质量

**关键数据结构**:

- `format`: 图片格式（'png' | 'jpeg'）
- `quality`: 图片质量（1-100，仅 JPEG）
- `fullPage`: 是否全页截图（暂未实现）

#### 4.10 `src/instructions/ExecuteScriptInstruction.ts`

**功能**: JavaScript 执行指令

**作用**:

- 在页面上下文中执行 JavaScript 代码
- 支持完整的 CDP Runtime.evaluate 参数

**关键数据结构**:

- `expression`: JavaScript 表达式
- `returnByValue`: 是否按值返回
- `awaitPromise`: 是否等待 Promise
- `userGesture`: 是否模拟用户手势
- 其他 CDP Runtime.evaluate 参数

#### 4.11 `src/instructions/WaitInstruction.ts`

**功能**: 等待指令

**作用**:

- 等待指定时间
- 等待元素出现
- 等待条件满足

**关键数据结构**:

- `time`: 等待时间（秒）
- `condition`: 等待条件（可选）

#### 4.12 `src/instructions/GetUrlInstruction.ts`

**功能**: 获取当前 URL 指令

**作用**:

- 获取当前标签页的 URL

**关键数据结构**: 无

#### 4.13 `src/instructions/ActivateTabInstruction.ts`

**功能**: 激活标签页指令

**作用**:

- 激活指定的标签页
- 使标签页成为当前活动标签页

**关键数据结构**:

- `tabId`: 要激活的标签页 ID

#### 4.14 `src/instructions/index.ts`

**功能**: 指令模块统一导出和工厂类

**作用**:

- 导出所有指令类
- 提供指令工厂方法，根据指令类型创建对应的指令实例

**关键数据结构**:

- `InstructionFactory`: 指令工厂类
  - `create(instruction)`: 根据指令对象创建指令实例
  - `toObject(instruction)`: 将指令对象转换为普通对象

**支持的指令类型**:

- `find_element`: FindElementInstructionClass
- `keyboard`: KeyboardInstructionClass
- `mouse`: MouseInstructionClass
- `input`: InputInstructionClass
- `get_attribute`: GetAttributeInstructionClass
- `set_attribute`: SetAttributeInstructionClass
- `navigate`: NavigateInstructionClass
- `screenshot`: ScreenshotInstructionClass
- `execute_script`: ExecuteScriptInstructionClass
- `wait`: WaitInstructionClass
- `get_url`: GetUrlInstructionClass
- `activate_tab`: ActivateTabInstructionClass

---

### 5. 管理器模块 (Managers)

#### 5.1 `src/managers/ElementManager.ts`

**功能**: 元素管理器

**作用**:

- 管理所有已找到的元素
- 按标签页 ID 和元素名称分组存储
- 提供元素的增删改查功能
- 提供元素定位功能

**关键数据结构**:

- `elements`: 元素映射（Map<tabId, Map<name, IElement>>）
- `ElementClass`: 元素类实现
  - `elementData`: 元素数据对象
  - `Validate()`: 验证元素数据
  - `GetNodeId()`: 获取节点 ID
  - `GetTag()`: 获取元素标记
  - `GetName()`: 获取元素名称
  - `LocateElement()`: 定位元素

**主要方法**:

- `GetElementByName(tabId, name)`: 根据名称获取元素
- `SetElementByName(tabId, name, element)`: 保存元素
- `RemoveElementByName(tabId, name)`: 删除元素
- `HasElementByName(tabId, name)`: 检查元素是否存在
- `GetElementCount(tabId)`: 获取元素数量
- `ClearTabElements(tabId)`: 清空指定标签页的所有元素
- `clearAll()`: 清空所有元素

**元素定位流程**:

1. 根据选择器类型查找所有匹配的元素
   - **CSS**: 使用 `DOM.querySelectorAll` 查找元素
   - **ID**: 使用 `DOM.querySelectorAll` 查找元素（自动添加 `#` 前缀）
   - **Tag**: 使用 `ElementTag` 属性选择器查找元素
   - **Text**: 使用 `Runtime.evaluate` 在页面上下文中查找包含指定文本的元素
   - **Ledby**: 使用 `Runtime.evaluate` 查找包含 `aria-labelledby` 属性的元素，然后通过 `aria-labelledby` 的值找到对应的 label 元素，检查 label 元素的文本内容是否包含指定文本
2. 使用相对关系筛选（父元素、子元素、兄弟元素）
3. 检查元素可见性（优先选择可见的元素）
4. 为选中的元素生成唯一的 tag
5. 通过 CDP 设置元素的 tag 属性
6. 保存元素到管理器

**Ledby 选择器工作原理**:

- 首先通过 `selector` 参数找到所有匹配的候选元素
- 筛选出包含 `aria-labelledby` 属性的元素
- 获取每个元素的 `aria-labelledby` 属性值
- 通过 `document.getElementById()` 找到对应的 label 元素
- 检查 label 元素的文本内容（`textContent` 或 `innerText`）是否包含 `text` 参数
- 如果匹配且元素可见，则返回该元素
- 适用于需要通过 label 文本来定位表单元素等场景

#### 5.2 `src/managers/InstructionManager.ts`

**功能**: 指令管理器

**作用**:

- 管理所有待执行的指令
- 按标签页 ID 分组存储
- 确保指令按创建时间排序（FIFO 队列）

**关键数据结构**:

- `instructionsMap`: 指令映射（Map<tabId, BaseInstructionClass[]>）

**主要方法**:

- `AddUnfilteredInstructions(instructions)`: 添加未过滤的指令列表（自动分组）
  - 按标签页分组指令
  - 调用 AddInstructions 添加到对应标签页
- `AddInstructions(tabId, instructions)`: 添加指令列表到指定标签页
  - 根据 `created_at` 排序（FIFO）
  - 添加到该标签页的指令列表
- `GetFirstInstructionByTabId(tabId)`: 获取第一个指令（FIFO）
  - 使用 `shift()` 从数组头部取出
  - 如果数组为空，删除对应的 key
- `GetInstructionsByTabId(tabId)`: 获取指定标签页的所有指令
- `GetAllTabIds()`: 获取所有标签页 ID
- `GetCountByTabId(tabId)`: 获取指定标签页的指令数量
- `DeleteInstructionsByTabId(tabId)`: 删除指定标签页的所有指令
- `DeleteInstructionsByCreatedAtBefore(tabId, created_at)`: 删除指定创建时间之前的指令

#### 5.3 `src/managers/InstructionResultManager.ts` (ResultManager)

**功能**: 指令结果管理器

**作用**:

- 保存所有指令的执行结果
- 按标签页 ID 分组存储结果

**关键数据结构**:

- `results`: 结果映射（Map<tabId, InstructionResult[]>）

**主要方法**:

- `SaveResult(result)`: 保存结果
- `GetResult(tabId)`: 获取指定标签页的结果
- `GetResultAndDelete(tabId)`: 获取并删除指定标签页的结果
- `GetAllResults()`: 获取所有结果
- `ClearResult(tabId)`: 清除指定标签页的结果
- `ClearAll()`: 清空所有结果

#### 5.4 `src/managers/NodeManager.ts` (NodeConfig)

**功能**: 节点配置管理器

**作用**:

- 管理节点配置信息
- 从浏览器本地存储读取和保存配置
- 自动生成节点 ID（UUID）

**关键数据结构**:

- `nodeProfile`: 节点配置对象（NodeProfile 类型）
  - `node_type`: 节点类型（默认 'crawler'）
  - `node_id`: 节点 ID（UUID，自动生成）
  - `node_name`: 节点名称（默认 'node'）
  - `node_token`: 节点令牌（默认值：'rjxu1QtB8z_N-WmeIHFEvmTAMmCyyseStW-\_UPrMzgk'）

**主要方法**:

- `GetNodeProfile()`: 获取节点配置（从本地存储或生成）
  - 如果本地存储中没有，生成默认值并保存
  - node_id 自动生成 UUID
- `UpdateNodeProfile(data)`: 更新节点配置
  - 更新 node_name 和 node_token
  - 保存到本地存储

**导出**:

- `nodeConfig`: 全局节点配置实例

---

### 6. 类型定义 (Types)

#### 6.1 `src/types/index.ts`

**功能**: 类型定义文件

**作用**:

- 导出所有 TypeScript 类型和接口
- 统一管理类型定义

**关键类型**:

- `NodeProfile`: 节点配置接口
- `TabInfo`: 标签页信息接口
- `ElementData`: 元素数据接口
- `InstructionResult`: 指令结果接口
- `InstructionResults`: 指令结果集合接口
- `BaseInstruction`: 基础指令接口
- `FindElementInstruction`: 查找元素指令接口
- `InputInstruction`: 输入指令接口
- `KeyboardInstruction`: 键盘指令接口
- `MouseInstruction`: 鼠标指令接口
- `GetAttributeInstruction`: 获取属性指令接口
- `SetAttributeInstruction`: 设置属性指令接口
- `NavigateInstruction`: 导航指令接口
- `ScreenshotInstruction`: 截图指令接口
- `ExecuteScriptInstruction`: 执行脚本指令接口
- `WaitInstruction`: 等待指令接口
- `GetUrlInstruction`: 获取 URL 指令接口
- `ActivateTabInstruction`: 激活标签页指令接口
- `Instruction`: 指令联合类型
- `ContentScriptMessageType`: 内容脚本消息类型
- `BackgroundScriptMessageType`: 背景脚本消息类型
- `PopupScriptMessageType`: Popup 脚本消息类型
- `WSMessage`: WebSocket 消息类型
- `CdpMessage`: CDP 消息类型
- `CdpResult`: CDP 结果类型
- `HttpMessage`: HTTP 消息类型
- `HttpResult`: HTTP 结果类型
- `ExecutorStatus`: 执行器状态接口

---

### 7. 工具函数 (Utils)

#### 7.1 `src/utils/index.ts`

**功能**: 工具函数集合

**作用**:

- 提供通用的工具函数
- 封装浏览器 API 调用
- 提供日志输出功能

**主要函数**:

- `GenerateUUID()`: 生成 UUID
- `GenerateRandomString(length)`: 生成随机字符串（默认 14 位）
- `EscapeCSSSelector(value)`: 转义 CSS 选择器
- `SendMessageToContentScript(tabId, message)`: 发送消息到内容脚本
- `SendMessageToBackgroundScript(message)`: 发送消息到背景脚本
- `SendMessageToPopupWindow(message)`: 发送消息到 popup 窗口
- `ExecuteCDPCommand(tabId, method, params)`: 执行 CDP 命令
  - 使用 `browser.debugger.sendCommand` API
  - 检查 API 层面的错误（runtime.lastError）
  - 检查 CDP 协议层面的错误（result.code）
  - 返回 CDP 命令执行结果
- `EnsureCDPConnected(tabId)`: 确保 CDP 已连接
  - 使用 `browser.debugger.attach` API
  - CDP 版本：1.3
  - 如果已经连接，忽略错误
- `DisconnectCDP(tabId)`: 断开 CDP 连接
  - 使用 `browser.debugger.detach` API
- `OutputLogToFile(message, options)`: 输出日志到文件
  - 使用 Chrome Native Messaging API
  - 支持日志级别（DEBUG、INFO、WARN、ERROR）
  - 自动添加时间戳
  - 日志缓冲机制（批量写入，提高性能）
  - 缓冲大小：10 条
  - 缓冲超时：5 秒
  - 自动重连机制
  - 错误处理和回退到控制台输出
- `FlushLogBuffer()`: 立即刷新日志缓冲
- `CloseLogConnection()`: 关闭日志连接

**日志级别**:

- `LogLevel.DEBUG`: 调试信息
- `LogLevel.INFO`: 一般信息
- `LogLevel.WARN`: 警告信息
- `LogLevel.ERROR`: 错误信息

**Native Messaging 应用名称**: `com.autojs.logger`

---

### 8. 常量配置 (Consts)

#### 8.1 `src/consts/index.ts`

**功能**: 常量定义文件

**作用**:

- 定义全局常量
- 从环境变量读取配置

**关键常量**:

- `ElementTag`: 元素标记常量 `'cdp-locate-id'`
  - 用于标记已找到的元素
  - 通过 CDP 设置到元素的属性上
- `WEBSOCKET_CONN_URL`: WebSocket 默认连接 URL
  - 从环境变量 `VITE_WEBSOCKET_CONN_URL` 读取
  - 默认值：`'wss://browser.autowave.dev/ws'`
- `DEBUG_MODE`: 调试模式
  - 从环境变量 `VITE_DEBUG_MODE` 读取
  - 默认值：`true`（如果环境变量未设置或为 'true'）
  - 控制是否输出日志

---

### 9. 示例文件

#### 9.1 `src/example.ts`

**功能**: 示例指令定义

**作用**:

- 提供各种指令类型的示例
- 用于测试和演示

**关键数据结构**:

- `example`: 示例指令数组，包含各种类型的指令示例

---

### 10. 配置文件

#### 10.1 `package.json`

**功能**: 项目依赖和脚本配置

**关键信息**:

- 项目名称: `web-crawler-extension`
- 主要依赖:
  - `vue`: Vue 3 框架
  - `wxt`: WXT 框架（Chrome 扩展开发框架）
  - `eventsource`: EventSource 支持
  - `node-fetch`: Fetch API 支持
- 开发依赖:
  - `typescript`: TypeScript 编译器
  - `vue-tsc`: Vue TypeScript 类型检查
  - `@wxt-dev/module-vue`: WXT Vue 模块
- 脚本命令:
  - `dev`: 开发模式（Chrome）
  - `dev:firefox`: 开发模式（Firefox）
  - `dev:edge`: 开发模式（Edge）
  - `build`: 构建扩展（Chrome）
  - `build:firefox`: 构建扩展（Firefox）
  - `build:edge`: 构建扩展（Edge）
  - `zip`: 打包扩展（Chrome）
  - `zip:firefox`: 打包扩展（Firefox）
  - `zip:edge`: 打包扩展（Edge）
  - `compile`: TypeScript 类型检查
  - `postinstall`: 安装后自动运行 `wxt prepare`

#### 10.2 `wxt.config.ts`

**功能**: WXT 框架配置文件

**关键配置**:

- `entrypointsDir`: 入口文件目录 `'./src/entrypoints'`
- `modules`: Vue 模块 `['@wxt-dev/module-vue']`
- `browser`: 默认浏览器 `'chrome'`
- `manifest`: 扩展清单配置
  - `version`: 版本号 `'1.0.2'`
  - `name`: 扩展名称 `'自动化网页'`
  - `description`: 扩展描述
  - `permissions`: 权限列表
    - `activeTab`: 访问当前活动标签页
    - `tabs`: 访问所有标签页
    - `storage`: 本地存储
    - `scripting`: 脚本注入
    - `alarms`: 定时任务
    - `debugger`: CDP 调试（必需）
    - `nativeMessaging`: Native Messaging（日志功能）
  - `host_permissions`: 主机权限 `['<all_urls>']`
  - `action`: 扩展图标配置
    - `default_title`: `'打开SemiAutoJs'`
- `dev`: 开发配置
  - `server.port`: 开发服务器端口 `3000`
- `vite`: Vite 配置
  - `build.sourcemap`: 生成源码映射
- `webExt`: Web 扩展配置
  - `binaries.chrome`: Chrome 可执行文件路径
  - `startUrls`: 启动 URL `['https://www.facebook.com']`
  - `chromiumProfile`: Chrome 用户数据目录
  - `keepProfileChanges`: 保持配置文件更改
  - `chromiumArgs`: Chrome 启动参数
    - `--remote-debugging-port=9222`: CDP 调试端口
    - `--no-first-run`: 跳过首次运行
    - `--no-default-browser-check`: 跳过默认浏览器检查
    - `--disable-features=VizDisplayCompositor`: 禁用显示合成器

#### 10.3 `tsconfig.json`

**功能**: TypeScript 配置文件

**关键配置**:

- 继承 WXT 的 TypeScript 配置

---

## 项目功能特性

### 1. 浏览器自动化控制

- **CDP 协议支持**: 通过 Chrome DevTools Protocol 完全控制浏览器
- **元素查找**: 支持 CSS、XPath、ID、Tag、Text、Ledby 六种选择器，支持相对关系筛选
- **鼠标操作**: 支持点击、双击、右键、悬停、移动等操作，支持鼠标轨迹模拟
- **键盘操作**: 支持按键按下、释放、输入等操作，支持特殊按键
- **文本输入**: 支持逐字符输入，支持中文字符和 Unicode
- **页面导航**: 支持页面跳转和加载等待
- **页面截图**: 支持 PNG 和 JPEG 格式截图
- **元素截图**: 支持截取指定元素的截图
- **JavaScript 执行**: 支持在页面上下文中执行 JavaScript 代码
- **元素属性操作**: 支持获取和设置元素属性

### 2. 指令执行系统

- **FIFO 队列**: 指令按创建时间（`created_at`）排序执行
- **重试机制**: 支持指令执行失败后自动重试
- **延迟控制**: 支持指令执行前后的延迟
- **错误处理**: 支持错误捕获和忽略
- **执行控制**: 支持暂停、恢复、停止执行
- **执行统计**: 统计执行数量、成功数、失败数
- **多标签页支持**: 支持同时管理多个标签页的指令
- **模块化设计**: 消息处理通过 handlers 模块组织，提高代码可维护性

### 3. WebSocket 通信

- **双向通信**: 支持客户端和服务器双向通信
- **自动重连**: 连接断开后自动重连（5 秒间隔）
- **心跳机制**: 定期发送心跳保持连接（15 秒间隔）
- **消息类型**: 支持登录、心跳、指令、结果、CDP、HTTP、标签页等多种消息类型
- **消息路由**: 根据消息类型自动路由到相应的处理器
- **消息大小限制**: 最大 10MB
- **登录认证**: 使用节点配置进行登录认证

### 4. 日志收集

- **控制台日志**: 收集页面的控制台输出（通过 Runtime.consoleAPICalled 事件）
- **网络日志**: 收集网络请求和响应信息（通过 Network 域事件）
- **日志过滤**: 支持按条件过滤日志
- **日志分组**: 网络日志支持按请求 ID 分组
- **日志分页**: 支持分页获取日志
- **日志限制**: 每个标签页最多保存 10000 条日志
- **日志清空**: 支持清空指定标签页的日志

### 5. HTTP 请求执行

- **HTTP 方法支持**: 支持 GET、POST、PUT、DELETE、PATCH、HEAD、OPTIONS
- **请求头支持**: 支持自定义请求头
- **请求体支持**: 支持字符串和对象格式的请求体（自动转换为 JSON）
- **超时控制**: 支持设置请求超时（默认 180 秒）
- **响应解析**: 自动解析 JSON 响应
- **错误处理**: 完善的错误处理和异常捕获

### 6. 用户界面

- **Popup 窗口**: 提供友好的用户界面
- **Side Panel**: 提供更大的界面空间（Chrome 扩展新特性）
- **节点配置**: 管理节点信息
- **指令配置**: 输入和管理指令集
- **执行控制**: 控制指令执行流程
- **日志查看**: 查看执行日志和结果

### 7. 反检测机制

- **隐藏 webdriver**: 隐藏 `navigator.webdriver` 属性，防止网站检测
- **真实轨迹模拟**: 模拟真实的鼠标移动轨迹
- **早期注入**: Content Script 在 `document_start` 阶段运行，尽早隐藏 webdriver 属性

### 8. 日志系统

- **Native Messaging**: 通过本地应用写入日志文件
- **日志缓冲**: 批量写入日志，提高性能
- **自动重连**: 连接断开后自动重连
- **日志级别**: 支持 DEBUG、INFO、WARN、ERROR 四个级别
- **时间戳**: 自动添加时间戳
- **调试模式**: 通过环境变量控制是否输出日志

### 9. Service Worker 生命周期管理

- **自动初始化**: 扩展安装/更新时自动初始化（通过 InitializationService）
- **定时任务**: 使用 alarms API 创建定时任务（每分钟检查一次 WebSocket 连接）
- **连接检查**: 定时检查 WebSocket 连接状态（通过 WebSocketService.checkAndConnect）
- **自动重连**: Service Worker 唤醒后自动重连 WebSocket（通过 InitializationService.handleAlarm）
- **模块化服务**: 使用 InitializationService 统一管理初始化逻辑和生命周期事件

---

## 项目结构

```
autojs/
├── src/
│   ├── entrypoints/          # 入口文件
│   │   ├── background.ts      # Background Script (Service Worker)
│   │   ├── content.ts         # Content Script
│   │   ├── popup.html         # Popup HTML
│   │   ├── popup/             # Popup 组件
│   │   │   ├── main.ts        # Popup 入口
│   │   │   ├── App.vue        # Popup 主组件
│   │   │   └── components/    # Popup 子组件
│   │   │       ├── NodeConfig.vue
│   │   │       ├── InstructionConfig.vue
│   │   │       ├── ExecutionControl.vue
│   │   │       └── ExecutionLog.vue
│   ├── executor/              # 执行器模块
│   │   ├── index.ts
│   │   ├── InstructionExecutor.ts
│   │   ├── CdpExecutor.ts
│   │   ├── HttpExecutor.ts
│   │   └── WebSocketConnector.ts
│   ├── instructions/          # 指令模块
│   │   ├── index.ts
│   │   ├── BaseInstruction.ts
│   │   ├── FindElementInstruction.ts
│   │   ├── InputInstruction.ts
│   │   ├── KeyboardInstruction.ts
│   │   ├── MouseInstruction.ts
│   │   ├── GetAttributeInstruction.ts
│   │   ├── SetAttributeInstruction.ts
│   │   ├── NavigateInstruction.ts
│   │   ├── ScreenshotInstruction.ts
│   │   ├── ExecuteScriptInstruction.ts
│   │   ├── WaitInstruction.ts
│   │   ├── GetUrlInstruction.ts
│   │   └── ActivateTabInstruction.ts
│   ├── managers/              # 管理器模块
│   │   ├── index.ts
│   │   ├── ElementManager.ts
│   │   ├── InstructionManager.ts
│   │   ├── InstructionResultManager.ts
│   │   └── NodeManager.ts
│   ├── types/                 # 类型定义
│   │   ├── index.ts
│   │   ├── background.ts
│   │   ├── content.ts
│   │   ├── popup.ts
│   │   ├── executor.ts
│   │   ├── instruction.ts
│   │   ├── node.ts
│   │   ├── tab.ts
│   │   ├── websocket.ts
│   │   ├── cdp.ts
│   │   └── http.ts
│   ├── utils/                 # 工具函数
│   │   └── index.ts
│   ├── consts/                # 常量配置
│   │   └── index.ts
│   └── example.ts             # 示例文件
├── native/                    # Native Messaging 应用
│   ├── com.autojs.logger.exe  # 日志应用可执行文件
│   ├── com.autojs.logger.json # 日志应用配置
│   └── logs/                  # 日志文件目录
├── package.json               # 项目配置
├── tsconfig.json             # TypeScript 配置
├── wxt.config.ts            # WXT 配置
└── 项目文档.md              # 本文档
```

---

## 安装和运行

### 1. 环境要求

- Node.js >= 16.0.0
- npm 或 yarn
- Chrome 浏览器（推荐最新版本）

### 2. 安装依赖

```bash
# 安装扩展依赖
npm install
```

### 3. 开发模式运行

```bash
# 启动扩展开发模式（会自动打开 Chrome 浏览器）
npm run dev
```

### 4. 构建扩展

```bash
# 构建 Chrome 扩展
npm run build

# 打包扩展（生成 .zip 文件）
npm run zip
```

### 5. 手动安装扩展

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `.output/chrome-mv3` 目录（开发模式）或 `output/chrome-mv3` 目录（构建后）

### 6. 配置节点信息

1. 点击扩展图标打开 Popup 窗口或 Side Panel
2. 在"节点配置"标签页中配置节点名称和令牌
3. 点击"保存"按钮

### 7. 连接 WebSocket 服务器

1. 在"执行控制"标签页中
2. 输入 WebSocket URL（默认：`wss://browser.autowave.dev/ws`）
3. 点击"测试连接"测试连接
4. 点击"连接"建立连接

### 8. 执行指令

1. 在"指令配置"标签页中
2. 选择目标标签页
3. 输入指令集 JSON（或点击"加载示例"）
4. 点击"发送指令"
5. 在"执行控制"标签页中点击"开始执行"

---

## 配置说明

### 环境变量

项目支持通过环境变量配置以下选项：

- `VITE_WEBSOCKET_CONN_URL`: WebSocket 连接 URL

  - 默认值：`wss://browser.autowave.dev/ws`
  - 示例：`ws://localhost:8080`

- `VITE_DEBUG_MODE`: 调试模式
  - 默认值：`true`（如果未设置或为 'true'）
  - 有效值：`'true'` 表示启用，其他值表示禁用
  - 控制是否输出日志到文件

### 节点配置

节点配置存储在浏览器本地存储中，包括：

- `node_type`: 节点类型（默认：'crawler'）
- `node_id`: 节点 ID（自动生成 UUID）
- `node_name`: 节点名称（默认：'node'）
- `node_token`: 节点令牌（默认值：'rjxu1QtB8z_N-WmeIHFEvmTAMmCyyseStW-\_UPrMzgk'）

### Chrome 启动参数

开发模式下，Chrome 使用以下启动参数：

- `--remote-debugging-port=9222`: CDP 调试端口
- `--no-first-run`: 跳过首次运行
- `--no-default-browser-check`: 跳过默认浏览器检查
- `--disable-features=VizDisplayCompositor`: 禁用显示合成器

---

## 注意事项

### 1. 权限要求

- 扩展需要以下权限：
  - `activeTab`: 访问当前活动标签页
  - `tabs`: 访问所有标签页
  - `storage`: 本地存储
  - `scripting`: 脚本注入
  - `alarms`: 定时任务
  - `debugger`: CDP 调试（必需）
  - `nativeMessaging`: Native Messaging（日志功能）
  - `<all_urls>`: 访问所有网站

### 2. CDP 连接

- 扩展使用 `browser.debugger` API 连接 CDP
- CDP 版本：1.3
- 如果其他扩展或 DevTools 已连接，可能会产生冲突
- 建议关闭其他可能使用 CDP 的扩展
- 连接失败时会忽略"另一个调试器已连接"的错误

### 3. 元素查找

- 元素查找使用 CDP DOM 域，需要先启用 `DOM.enable`
- 找到的元素会设置一个唯一的标记（tag），用于后续操作
- 如果多个元素匹配，会优先选择可见的元素
- 支持通过相对关系（父元素、子元素、兄弟元素）进一步筛选
- 支持六种选择器类型：CSS、XPath、ID、Tag、Text、Ledby

### 4. 指令执行

- 指令按创建时间（`created_at`）排序执行（FIFO 队列）
- 每个指令可以设置延迟（`delay`）、重试次数（`retry`）、超时时间（`timeout`）
- 指令执行失败会根据 `retry` 设置自动重试
- 如果设置了 `ignoreError`，即使失败也会继续执行下一条指令
- 执行循环最多进行 10 轮，每轮处理所有标签页的指令
- 执行完成后会自动发送结果到服务器

### 5. WebSocket 通信

- WebSocket 连接需要先登录（发送节点配置）
- 连接断开后会自动重连（5 秒间隔）
- 心跳间隔为 15 秒
- 消息大小限制为 10MB
- Service Worker 休眠时连接会断开，唤醒后会自动重连

### 6. 鼠标轨迹模拟

- `calculated`: 使用贝塞尔曲线模拟轨迹（默认）
- `simulated`: 使用更复杂的算法模拟真实轨迹
- `none`: 不模拟轨迹，直接移动到目标位置

### 7. 文本输入

- 文本输入使用 CDP `Input.dispatchKeyEvent` 的 `char` 类型事件
- 支持中文字符和 Unicode 字符（包括 emoji）
- 可以设置 `delay` 来控制每个字符之间的输入间隔

### 8. 反检测

- Content Script 在 `document_start` 阶段运行，尽早隐藏 `webdriver` 属性
- 使用真实的鼠标轨迹模拟，减少被检测的风险
- 但完全避免检测是不可能的，某些网站仍可能检测到自动化工具

### 9. 性能考虑

- 每个标签页最多保存 10000 条控制台日志和网络日志
- 指令执行是同步的，大量指令可能需要较长时间
- 建议合理设置指令的延迟和重试次数
- 日志使用缓冲机制，批量写入提高性能

### 10. 错误处理

- 所有错误都会被捕获并记录到执行结果中
- 可以通过 `ignoreError` 选项忽略某些错误
- 执行日志会显示所有成功和失败的指令
- 标签页不存在时会自动清理该标签页的指令

### 11. Service Worker 生命周期

- Chrome 扩展使用 Service Worker 作为 Background Script
- Service Worker 可能会休眠以节省资源
- 使用 alarms API 创建定时任务（每分钟执行一次），定期唤醒 Service Worker
- 唤醒后通过 InitializationService.handleAlarm 处理定时任务
- 定时任务会调用 WebSocketService.checkAndConnect 检查并修复 WebSocket 连接状态
- 初始化逻辑通过 InitializationService 统一管理，确保幂等性

### 12. 日志系统

- 日志通过 Native Messaging API 写入本地文件
- Native Messaging 应用名称：`com.autojs.logger`
- 日志缓冲机制：10 条日志或 5 秒超时后批量写入
- 连接断开后会自动重连
- 如果 Native Messaging 不可用，会回退到控制台输出

### 13. 开发建议

- 使用 TypeScript 的类型检查确保代码质量
- 遵循项目的代码结构和命名规范
- 添加新指令类型时，需要在 `InstructionFactory` 中注册
- 测试时可以使用 `example.ts` 中的示例指令
- 使用 `OutputLogToFile` 记录日志，便于调试

---

## API 接口说明

### Background Script 消息接口

#### get_tabs

获取所有标签页列表。

**请求**:

```typescript
{
  type: "get_tabs";
}
```

**响应**:

```typescript
{
  success: true,
  data: TabInfo[]
}
```

#### get_node_profile

获取节点配置。

**请求**:

```typescript
{
  type: "get_node_profile";
}
```

**响应**:

```typescript
{
  success: true,
  data: NodeProfile
}
```

#### update_node_profile

更新节点配置。

**请求**:

```typescript
{
  type: 'update_node_profile',
  params: {
    node_name?: string,
    node_token?: string
  }
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### add_instructions

添加指令集。

**请求**:

```typescript
{
  type: 'add_instructions',
  params: {
    tabId: number,
    instructionsJsonString: string  // JSON 格式的指令字符串
  }
}
```

**响应**:

```typescript
{
  success: true,
  count: number  // 添加的指令数量
}
```

#### execute_instructions

执行指令集。

**请求**:

```typescript
{
  type: 'execute_instructions',
  params: {
    tabId: number
  }
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### pause_execution

暂停执行。

**请求**:

```typescript
{
  type: "pause_execution";
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### stop_execution

停止执行。

**请求**:

```typescript
{
  type: "stop_execution";
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### get_executor_status

获取执行器状态。

**请求**:

```typescript
{
  type: "get_executor_status";
}
```

**响应**:

```typescript
{
  success: true,
  data: ExecutorStatus
}
```

#### get_results

获取执行结果。

**请求**:

```typescript
{
  type: "get_results";
}
```

**响应**:

```typescript
{
  success: true,
  data: InstructionResult[]
}
```

#### clear_results

清空执行结果。

**请求**:

```typescript
{
  type: "clear_results";
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### connect_websocket

连接 WebSocket。

**请求**:

```typescript
{
  type: 'connect_websocket',
  params: {
    url: string
  }
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### disconnect_websocket

断开 WebSocket。

**请求**:

```typescript
{
  type: "disconnect_websocket";
}
```

**响应**:

```typescript
{
  success: true;
}
```

#### test_websocket

测试 WebSocket 连接。

**请求**:

```typescript
{
  type: 'test_websocket',
  params: {
    url: string
  }
}
```

**响应**:

```typescript
{
  success: true,
  data: {
    connected: boolean
  }
}
```

#### send_results_to_server

发送执行结果到服务器。

**请求**:

```typescript
{
  type: "send_results_to_server";
}
```

**响应**:

```typescript
{
  success: true;
}
```

### Content Script 消息接口

#### scroll_into_view

滚动元素到视图。

**请求**:

```typescript
{
  type: 'scroll_into_view',
  params: {
    tag: string
  }
}
```

**响应**:

```typescript
{
  success: boolean;
}
```

#### get_attribute

获取元素属性。

**请求**:

```typescript
{
  type: 'get_attribute',
  params: {
    tag: string,
    attribute?: string  // 默认为 'text'
  }
}
```

**响应**:

```typescript
{
  success: boolean,
  data?: string
}
```

#### get_text

获取元素文本。

**请求**:

```typescript
{
  type: 'get_text',
  params: {
    tag: string
  }
}
```

**响应**:

```typescript
{
  success: boolean,
  data?: string
}
```

#### is_visible

检查元素是否可见。

**请求**:

```typescript
{
  type: 'is_visible',
  params: {
    tag: string
  }
}
```

**响应**:

```typescript
{
  success: boolean,
  data: boolean
}
```

### WebSocket 消息接口

#### login

登录消息。

**请求**:

```typescript
{
  type: 'login',
  data: NodeProfile
}
```

**响应**:

```typescript
{
  type: 'login',
  data: {
    success: boolean,
    node_id?: string,
    error?: string,
    message?: string
  }
}
```

#### heartbeat

心跳消息。

**请求**:

```typescript
{
  type: 'heartbeat',
  data: {
    timestamp: number
  }
}
```

**响应**:

```typescript
{
  type: 'heartbeat',
  data: {
    success: boolean
  }
}
```

#### instructions

指令消息。

**请求**:

```typescript
{
  type: 'instructions',
  data: BaseInstruction[]
}
```

**说明**: 指令会被自动执行，执行结果会通过 WebSocket 返回。

#### cdp

CDP 消息。

**请求**:

```typescript
{
  type: 'cdp',
  data: CdpMessage
}
```

**响应**:

```typescript
{
  type: 'cdp',
  data: CdpResult
}
```

#### http

HTTP 消息。

**请求**:

```typescript
{
  type: 'http',
  data: HttpMessage
}
```

**响应**:

```typescript
{
  type: 'http',
  data: HttpResult
}
```

#### tabs

标签页消息。

**请求/响应**:

```typescript
{
  type: 'tabs',
  data: TabInfo
}
```

**说明**: 标签页激活、更新、关闭时会自动发送消息到服务器。

---

## 总结

AutoJS 是一个功能完整的 Web 自动化爬虫 Chrome 扩展，通过 CDP 协议实现浏览器控制，支持丰富的操作指令，提供友好的用户界面，并通过 WebSocket 与服务器通信。项目采用模块化设计，代码结构清晰，易于扩展和维护。

### 核心优势

1. **完整的浏览器控制**: 通过 CDP 协议实现全面的浏览器自动化
2. **灵活的指令系统**: 支持多种指令类型，可组合使用
3. **可靠的通信机制**: WebSocket 双向通信，自动重连，心跳保活
4. **强大的日志系统**: 控制台日志、网络日志收集和管理
5. **友好的用户界面**: Popup 和 Side Panel 双界面支持
6. **完善的错误处理**: 重试机制、错误捕获、异常处理
7. **Service Worker 生命周期管理**: 自动处理休眠和唤醒
8. **模块化架构**: 后台脚本采用 handlers 和 services 模块化组织，提高代码可维护性和可测试性

### 适用场景

- Web 自动化测试
- 网页数据采集
- 自动化操作流程
- 浏览器行为模拟
- 网页监控和分析

---

**文档版本**: 2.1  
**最后更新**: 2026-02-05  
**维护者**: AutoJS 开发团队

**更新说明**:

- 更新了 background.ts 部分，反映模块化的 handlers 和 services 架构
- 更新了可见性检查标准，从 10 项扩展到 13 个维度
- 更新了 execute_script 消息类型说明，标注当前未实现状态
- 更新了 Service Worker 生命周期管理说明，反映 InitializationService 的使用
- 更新了 WebSocket 连接流程说明，反映 WebSocketService 的回调机制
